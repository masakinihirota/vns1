import Ssg from '../../pages/ssg'
import { render } from '@testing-library/react'

// コンポーネント以外での場所のテスト 表示した文字列のみ
describe('ssg.tsxコンポーネント', () => {
  test('should first', () => {
    // SSGのテストの書き方がわからない
    // const { getByText } = render(<Ssg />)
    // expect(getByText('龍と苺')).toBeTruthy()
  })
})
